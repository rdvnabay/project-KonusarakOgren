@model List<QuizDto>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Başlık</th>
            <th scope="col">Tarih</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var quiz in Model)
        {
            <tr>
                <td scope="row">@quiz.Id</td>
                <td>@quiz.Question</td>
                <td>@quiz.CorrectAnswer</td>
                <td><a class="btn btn-danger" asp-controller="User" asp-action="Quiz" asp-route-quizId="@quiz.Id">Sınava Gir</a></td>
            </tr>
        }
    </tbody>
</table>